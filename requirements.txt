# Mirrors backend/requirements.txt — kept here too because Vercel's Python
# runtime auto-detects dependencies from a requirements.txt at the project
# root. Keep the two files in sync when dependencies change.
fastapi==0.139.2
uvicorn==0.51.0
Jinja2==3.1.6
pandas==3.0.3
openpyxl==3.1.5
python-multipart==0.0.32
python-dotenv==1.2.2
anthropic==0.120.0
psycopg[binary]==3.3.4
httpx==0.28.1
